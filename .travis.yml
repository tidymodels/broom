# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
sudo: required
cache: packages

r_binary_packages:
  - rstanarm
  - rstan

after_success:
  - Rscript -e 'covr::codecov()'

bioc_required: true
