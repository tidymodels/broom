- column: .class
  description:
  - The class assigned by the Mclust algorithm
  - Predicted class, using modal assignment
  used_by:
  - Mclust
  - poLCA
- column: .cluster
  description: The cluster assigned by the k-means algorithm
  used_by: kmeans
- column: .col.prop
  description: Column proportion (2 dimensions table only)
  used_by: htest
- column: .conf.high
  description:
  - Upper confidence interval value  `predict.rq` does not provide confidence intervals
    when `newdata` is provided.
  - Upper confidence interval value  `predict.rqs` does not provide confidence intervals
    when `newdata` is provided.
  used_by:
  - rq
  - rqs
- column: .conf.low
  description:
  - Lower confidence interval value
  - Lower confidence interval value
  used_by:
  - rq
  - rqs
- column: .cooksd
  description:
  - See [=cooks.distance::cooks.distance()()].  For additional details on Cook's distance,
    see [stats:cooks.distance::stats::cooks.distance()()].
  - Cooks distance, [=cooks.distance::cooks.distance()()]
  - Cooks distance, [=cooks.distance::cooks.distance()()]
  - Cooks distance, [=cooks.distance::cooks.distance()()]
  - Cooks distance, [=cooks.distance::cooks.distance()()]
  - Cooks distance, [=cooks.distance::cooks.distance()()]
  used_by:
  - betareg
  - glm
  - glmRob
  - lm
  - lmRob
  - rlm
- column: .expected
  description: Expected count under the null hypothesis
  used_by: htest
- column: .fitted
  description:
  - TODO
  - Fitted values of model
  - The predicted response for that observation.
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - Fitted values of model
  - The predicted response for that observation.
  - The predicted response for that observation.
  - The predicted response for that observation.
  - Fitted values of model
  - Fitted values of model
  - Fitted quantiles of the model
  - Fitted quantiles of the model
  - The predicted response for that observation.
  - Fitted values of model
  used_by:
  - betareg
  - coxph
  - felm
  - glm
  - glm
  - glmRob
  - glmRob
  - lm
  - lm
  - lmRob
  - lmRob
  - loess
  - loess
  - nlrq
  - nls
  - plm
  - rlm
  - rlm
  - rq
  - rqs
  - smooth.spline
  - survreg
- column: .fitted_j_0
  description: population-level fitted values for the j-th longitudinal process
  used_by: mjoint
- column: .fitted_j_1
  description: individuals-level fitted values for the j-th longitudinal process
  used_by: mjoint
- column: .hat
  description:
  - Diagonal of the hat matrix
  - Diagonal of the hat matrix
  - Diagonal of the hat matrix
  - Diagonal of the hat matrix
  - Diagonal of the hat matrix
  used_by:
  - glm
  - glmRob
  - lm
  - lmRob
  - rlm
- column: .observed
  description: Observed count
  used_by: htest
- column: .probability
  description: Posterior probability of predicted class  If the `data` argument is
    given, those columns are included in the output (only rows for which predictions
    could be made). Otherwise, the `y` element of the poLCA object, which contains
    the manifest variables used to fit the model, are used, along with any covariates,
    if present, in `x`.  Note that while the probability of all the classes (not just
    the predicted modal class) can be found in the `posterior` element, these are
    not included in the augmented output.
  used_by: poLCA
- column: .prop
  description: Proportion of the total
  used_by: htest
- column: .resid
  description:
  - TODO
  - Residuals (not present if `newdata` specified.)
  - The residual for a particular point. Present only when data has been passed to
    `augment` via the `data` argument.
  - Residuals
  - Residuals of fitted values on the new data
  - Residuals
  - Residuals of fitted values on the new data
  - Residuals
  - Residuals of fitted values on the new data
  - Residuals
  - Residuals of fitted values on the new data
  - 'Residuals of the fitted values  When `newdata` is supplied `augment.loess` returns
    one row for each observation with one additional column: '
  - The residual for a particular point. Present only when data has been passed to
    `augment` via the `data` argument.
  - The residual for a particular point. Present only when data has been passed to
    `augment` via the `data` argument.
  - The residual for a particular point. Present only when data has been passed to
    `augment` via the `data` argument.
  - Residuals
  - Residuals of fitted values on the new data
  - Residuals
  - Residuals
  - The residual for a particular point. Present only when data has been passed to
    `augment` via the `data` argument.
  - Residuals
  used_by:
  - betareg
  - coxph
  - felm
  - glm
  - glm
  - glmRob
  - glmRob
  - lm
  - lm
  - lmRob
  - lmRob
  - loess
  - nlrq
  - nls
  - plm
  - rlm
  - rlm
  - rq
  - rqs
  - smooth.spline
  - survreg
- column: .resid_j_0
  description: population-level residuals for the j-th longitudinal process
  used_by: mjoint
- column: .resid_j_1
  description: individual-level residuals for the j-th longitudinal process
  used_by: mjoint
- column: .residuals
  description: Pearson residual
  used_by: htest
- column: .row.prop
  description: Row proportion (2 dimensions table only)
  used_by: htest
- column: .se.fit
  description:
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of the fitted values
  - Standard errors of the fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  - Standard errors of fitted values
  used_by:
  - coxph
  - glm
  - glm
  - glmRob
  - glmRob
  - lm
  - lm
  - lmRob
  - lmRob
  - loess
  - loess
  - rlm
  - rlm
  - survreg
- column: .sigma
  description:
  - Estimate of residual standard deviation when corresponding observation is dropped
    from model
  - Estimate of residual standard deviation when corresponding observation is dropped
    from model
  - Estimate of residual standard deviation when corresponding observation is dropped
    from model
  - Estimate of residual standard deviation when corresponding observation is dropped
    from model
  - Estimate of residual standard deviation when corresponding observation is dropped
    from model
  used_by:
  - glm
  - glmRob
  - lm
  - lmRob
  - rlm
- column: .std.resid
  description:
  - 'Standardised residuals  Some unusual `lm` objects, such as `rlm` from MASS, may
    omit `.cooksd` and `.std.resid`. `gam` from mgcv omits `.sigma`.  When `newdata`
    is supplied, returns one row for each observation, with three columns added to
    the new data: '
  - 'Standardised residuals  Some unusual `lm` objects, such as `rlm` from MASS, may
    omit `.cooksd` and `.std.resid`. `gam` from mgcv omits `.sigma`.  When `newdata`
    is supplied, returns one row for each observation, with three columns added to
    the new data: '
  - 'Standardised residuals  Some unusual `lm` objects, such as `rlm` from MASS, may
    omit `.cooksd` and `.std.resid`. `gam` from mgcv omits `.sigma`.  When `newdata`
    is supplied, returns one row for each observation, with three columns added to
    the new data: '
  - 'Standardised residuals  Some unusual `lm` objects, such as `rlm` from MASS, may
    omit `.cooksd` and `.std.resid`. `gam` from mgcv omits `.sigma`.  When `newdata`
    is supplied, returns one row for each observation, with three columns added to
    the new data: '
  - 'Standardised residuals  Some unusual `lm` objects, such as `rlm` from MASS, may
    omit `.cooksd` and `.std.resid`. `gam` from mgcv omits `.sigma`.  When `newdata`
    is supplied, returns one row for each observation, with three columns added to
    the new data: '
  used_by:
  - glm
  - glmRob
  - lm
  - lmRob
  - rlm
- column: .stdres
  description: Standardized residual
  used_by: htest
- column: .tau
  description:
  - 'Quantile estimated  Depending on the arguments passed on to `predict.rq` via
    `...`, a confidence interval is also calculated on the fitted values resulting
    in columns:'
  - 'Quantile estimated  Depending on the arguments passed on to `predict.rqs` via
    `...`, a confidence interval is also calculated on the fitted values resulting
    in columns:'
  used_by:
  - rq
  - rqs
- column: .uncertainty
  description: The uncertainty associated with the classification. If a point has
    a probability of 0.9 of being in its assigned class under the model, then the
    uncertainty is 0.1.
  used_by: Mclust
- column: '`.remainder`'
  description:
  - The remainder, or "random" component of the decomposition.
  - The remainder, or "random" component of the decomposition.
  used_by:
  - decomposed.ts
  - stl
- column: '`.seasadj`'
  description:
  - The seasonally adjusted (or "deseasonalised") series.
  - The seasonally adjusted (or "deseasonalised") series.
  used_by:
  - decomposed.ts
  - stl
- column: '`.seasonal`'
  description:
  - The seasonal component of the decomposition.
  - The seasonal component of the decomposition.
  used_by:
  - decomposed.ts
  - stl
- column: '`.trend`'
  description:
  - The trend component of the decomposition.
  - The trend component of the decomposition.
  used_by:
  - decomposed.ts
  - stl
- column: '`.weight`'
  description:
  - The final robust weights (`stl` only).
  - The final robust weights, if requested.
  used_by:
  - decomposed.ts
  - stl
