# augment.drc

    Code
      augment(mod)
    Condition
      Error:
      ! Must specify either `data` or `newdata` argument.

---

    Code
      augment(mod2)
    Condition
      Error:
      ! Must specify either `data` or `newdata` argument.

