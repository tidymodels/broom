#' @param quick Logical indiciating if the only the `term` and `estimate`
#'   columns should be returned. Often useful to avoid time consuming
#'   covariance calculations. Defaults to `FALSE`.
#' @md
